如果你使用了lombok插件的@Data去生成getter，setter等
同时也使用了swagger去进行接口管理
那么，此代码就相当适合你

我相信很多人都用过逆向工程，但是我们有时候并不需要他生成的mapper，xml等，其实很多适合我们只需要model
但是逆向工程生成的model，一定情况下我们还是需要修改，如：使用了swagger的时候

本代码主要是解决此问题，当然也可以用于普通情况下的model生成，当然需要你自己修改下对应代码
大家在项目管理的时候，可以使用powerdesigner去设计表结构，然后生成sql文件导入数据库（mysql），最后可以使用该工具去生成Java的Model类
（主要是当表多的时候，写model真的要人命）

代码花了点业余时间，随意写了下，可能会有bug问题，大家可以发到邮箱576948461@qq.com告知
